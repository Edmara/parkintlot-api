pipelines:
  default:
    - step:
        name: Build
        image: microsoft/dotnet:2.2
        script:
          - dotnet restore
          - dotnet build
          - dotnet test
        artifacts:
          - ParkingLotApi/bin/**
          - ParkingLotApi.Tests/bin/**